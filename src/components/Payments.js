import React from 'react';

const Payments = () => {
    const handlePayment = () => {
        // Logika płatności
    };

    return (
        <div>
            <h2>Payments</h2>
            <button onClick={handlePayment}>Pay Now</button>
        </div>
    );
};

export default Payments;
